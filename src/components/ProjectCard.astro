---
export interface Props {
  title: string;
  description: string;
  tags: string[];
  githubUrl: string;
  demoUrl?: string | undefined;
}

const { title, description, tags, githubUrl, demoUrl } = Astro.props;
---

<div class="project-card">
  <div class="card-content stack">
    <h3>{title}</h3>
    <p class="description">{description}</p>
    <div class="tags">
      {tags.map((tag) => <span class="tag">{tag}</span>)}
    </div>
  </div>

  <div class="card-actions">
    <a
      href={githubUrl}
      class="action-button github"
      target="_blank"
      rel="noopener noreferrer"
    >
      <span class="icon">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          width="20"
          height="20"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
        >
          <path
            d="M15 22v-4a4.8 4.8 0 0 0-1-3.5c3 0 6-2 6-5.5.08-1.25-.27-2.48-1-3.5.28-1.15.28-2.35 0-3.5 0 0-1 0-3.5 1.5a13.4 13.4 0 0 0-7 0C6 2 5 2 5 2c-.3 1.15-.3 2.35 0 3.5A5.4 5.4 0 0 0 4 9c0 3.5 3 5.5 6 5.5-.39.49-.68 1.05-.85 1.65-.17.6-.22 1.23-.15 1.85v4"
          ></path>
          <path d="M9 18c-4.51 2-5-2-7-2"></path>
        </svg>
      </span>
      Code
    </a>
    {
      demoUrl && (
        <a
          href={demoUrl}
          class="action-button demo"
          target="_blank"
          rel="noopener noreferrer"
        >
          <span class="icon">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              width="20"
              height="20"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <polyline points="15 3 21 3 21 9" />
              <polyline points="9 21 3 21 3 15" />
              <line x1="21" y1="3" x2="14" y2="10" />
              <line x1="3" y1="21" x2="10" y2="14" />
            </svg>
          </span>
          Demo
        </a>
      )
    }
  </div>
</div>

<style>
  .project-card {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    height: 100%;
    background-color: #1a1a1a;
    /* Fluid border radius: scales with card size */
    border-radius: clamp(0.5rem, 2vw, 1rem);
    overflow: hidden;
    /* Fluid padding: responsive to container size */
    padding: clamp(0.75rem, 2vw, 1.5rem);
    transition:
      transform 0.2s ease,
      box-shadow 0.2s ease,
      border-color 0.2s ease;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.15);
    min-height: 0;
  }

  .project-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
    border-color: rgba(255, 255, 255, 0.25);
  }

  .card-content {
    display: flex;
    flex-direction: column;
    flex: 1;
    padding: 1.5rem;
  }

  h3 {
    /* Fluid typography: scales from 1.25rem to 2rem */
    font-size: clamp(1.25rem, 3vw, 2rem);
    font-weight: 700;
    margin: 0 0 0.75rem 0;
    color: white;
  }

  .description {
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
    max-height: 4.5rem;
    color: rgba(255, 255, 255, 0.7);
    font-size: 0.95rem;
    line-height: 1.5;
    margin-bottom: 1.25rem;
  }

  .tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-top: auto;
  }

  .tag {
    font-size: 0.75rem;
    font-weight: 500;
    padding: 0.35rem 0.75rem;
    border-radius: 9999px;
    background-color: rgba(255, 255, 255, 0.1);
    color: rgba(255, 255, 255, 0.9);
    transition: background-color 0.2s ease;
  }

  .tag:hover {
    background-color: rgba(255, 255, 255, 0.15);
  }

  .card-actions {
    display: flex;
    padding: 1rem 1.5rem;
    gap: 0.75rem;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
  }

  .action-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    padding: 0.5rem 1rem;
    border-radius: 8px;
    font-weight: 500;
    font-size: 0.875rem;
    transition: all 0.2s ease;
    text-decoration: none;
  }

  .github {
    background-color: #24292e;
    color: white;
  }

  .github:hover {
    background-color: #2c3136;
  }

  .demo {
    background-color: #3a3a3a;
    color: white;
  }

  .demo:hover {
    background-color: #454545;
  }

  .icon {
    display: inline-flex;
  }
</style>
