#!/bin/sh

echo "🔍 Running pre-commit checks..."

# Run comprehensive checks (TypeScript, linting, formatting)
echo "🔬 Running comprehensive checks (TypeScript + lint + format)..."
if ! deno task check-all; then
    echo "❌ Comprehensive checks failed. Fix the issues above."
    exit 1
fi

# Run tests
echo "🧪 Running tests..."
if ! deno test --allow-read --allow-env; then
    echo "❌ Tests failed. Fix the failing tests above."
    exit 1
fi

echo "✅ All pre-commit checks passed!"